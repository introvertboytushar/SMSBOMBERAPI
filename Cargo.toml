use worker::*;
use serde_json::{json, Value};
[package]
name = "smsbomber"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = { version = "0.4" }
serde_json = "1"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
EOF
